node_modules/
harnesses/java/gson.jar
harnesses/java/build/
fixtures/java/hello/lambda-core.jar
fixtures/java/hello/build/
# Fixtures ship a committed dist/ and invoke without an install, so their
# lockfiles are only a by-product of `npm run install:fixtures`.
fixtures/**/package-lock.json
__pycache__/
*.pyc

web/node_modules/
web/.output/
web/.nitro/
web/.tanstack/
web/dist/
web/src/routeTree.gen.ts

# Local gstack tooling state (daemon pid/port, auth token, browse logs)
.gstack/
